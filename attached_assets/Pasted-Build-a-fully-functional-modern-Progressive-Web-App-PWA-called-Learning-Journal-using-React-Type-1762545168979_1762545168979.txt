Build a fully functional, modern Progressive Web App (PWA) called Learning Journal using React, TypeScript, and Vite.

The app should be advanced, responsive, and installable, with offline support, data caching, and a professional UI.

🧠 Features:

Add, edit, and delete journal entries (title, content, date, tags)

Create and display projects with tech stacks and descriptions

Search and filter journals by tag or keyword

Offline-first with vite-plugin-pwa and IndexedDB (via localforage)

Dark/light theme toggle

Responsive UI using Tailwind CSS + ShadCN/UI components

React Router for page navigation (Home, Journal, Projects, About)

React Query (TanStack Query) for data caching and API simulation

Include a 404 page redirect for GitHub Pages

Add Framer Motion animations for smooth transitions

Add a Markdown editor (react-mde + react-markdown) for journal input

Include charts (Recharts or Chart.js) on the Home page for weekly reflection

Auto-sync journal entries when online again

SEO-friendly meta tags and PWA manifest

🧩 Tech Stack:

React 18 + TypeScript

Vite

Tailwind CSS

ShadCN/UI

React Router DOM

React Query

localforage

vite-plugin-pwa

Framer Motion

react-markdown + react-mde

Recharts or Chart.js

📁 Project Structure:
client/
├── public/
│   ├── favicon.png
│   ├── manifest.webmanifest
│   └── 404.html
├── src/
│   ├── main.tsx
│   ├── App.tsx
│   ├── index.css
│   ├── components/
│   │   ├── Navbar.tsx
│   │   ├── Footer.tsx
│   │   ├── JournalCard.tsx
│   │   ├── ProjectCard.tsx
│   │   └── ui/
│   ├── pages/
│   │   ├── HomePage.tsx
│   │   ├── JournalPage.tsx
│   │   ├── ProjectsPage.tsx
│   │   ├── AboutPage.tsx
│   │   └── NotFoundPage.tsx
│   ├── hooks/
│   │   ├── useOfflineStatus.ts
│   │   └── useJournalStore.ts
│   ├── lib/
│   │   ├── db.ts
│   │   ├── api.ts
│   │   └── utils.ts
│   └── assets/
│       └── images/
└── vite.config.ts

🧠 Requirements:

Use modern ES modules and hooks

Fully typed with TypeScript

Build optimized for GitHub Pages deployment (base: "/learning-journal-pwa/")

Include service worker for caching and offline mode

Visually polished dashboard with analytics and progress summary

Must run perfectly in Replit’s webview

🎯 Goal:

Deliver a modern, installable, offline-first learning journal web app — visually elegant, fast, and feature-rich.