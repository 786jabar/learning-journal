<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lab 4: API Integration Demo - Vanilla JavaScript</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 'Oxygen', sans-serif;
            line-height: 1.6;
            color: #1f2937;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 2rem 1rem;
        }
        
        .container {
            max-width: 1400px;
            margin: 0 auto;
            background: #ffffff;
            border-radius: 24px;
            padding: 3rem;
            box-shadow: 0 25px 50px rgba(0, 0, 0, 0.3);
        }
        
        header {
            text-align: center;
            margin-bottom: 3rem;
            padding-bottom: 2rem;
            border-bottom: 3px solid #f3f4f6;
        }
        
        h1 {
            font-size: 2.5rem;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-bottom: 0.5rem;
        }
        
        .subtitle {
            color: #6b7280;
            font-size: 1.1rem;
        }
        
        .badge {
            display: inline-block;
            background: #667eea;
            color: white;
            padding: 0.25rem 0.75rem;
            border-radius: 20px;
            font-size: 0.875rem;
            margin-top: 1rem;
        }
        
        .section {
            margin-bottom: 4rem;
        }
        
        .section-header {
            display: flex;
            align-items: center;
            gap: 1rem;
            margin-bottom: 2rem;
            padding-bottom: 1rem;
            border-bottom: 2px solid #e5e7eb;
        }
        
        .section-header h2 {
            color: #1f2937;
            font-size: 1.875rem;
            margin: 0;
        }
        
        .section-badge {
            background: #f3f4f6;
            color: #374151;
            padding: 0.5rem 1rem;
            border-radius: 8px;
            font-size: 0.875rem;
            font-weight: 600;
        }
        
        .demo-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 2rem;
        }
        
        .demo-card {
            background: #f9fafb;
            border: 2px solid #e5e7eb;
            border-radius: 16px;
            padding: 2rem;
            transition: all 0.3s ease;
        }
        
        .demo-card:hover {
            border-color: #667eea;
            box-shadow: 0 8px 20px rgba(102, 126, 234, 0.15);
            transform: translateY(-2px);
        }
        
        .demo-card h3 {
            color: #667eea;
            font-size: 1.25rem;
            margin-bottom: 0.75rem;
        }
        
        .description {
            color: #6b7280;
            font-size: 0.9rem;
            margin-bottom: 1.5rem;
            line-height: 1.5;
        }
        
        .instructions {
            background: #fff;
            border-left: 4px solid #667eea;
            padding: 1rem;
            margin-bottom: 1.5rem;
            border-radius: 4px;
        }
        
        .instructions h4 {
            color: #667eea;
            font-size: 0.875rem;
            margin-bottom: 0.5rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        
        .instructions ol {
            margin-left: 1.25rem;
            color: #4b5563;
            font-size: 0.9rem;
        }
        
        .instructions li {
            margin-bottom: 0.25rem;
        }
        
        button {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
            padding: 0.875rem 1.75rem;
            border-radius: 10px;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.2s ease;
            width: 100%;
            margin-bottom: 1rem;
        }
        
        button:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 16px rgba(102, 126, 234, 0.4);
        }
        
        button:active {
            transform: translateY(0);
        }
        
        input[type="text"],
        textarea {
            width: 100%;
            padding: 0.875rem;
            border: 2px solid #e5e7eb;
            border-radius: 8px;
            font-size: 1rem;
            margin-bottom: 1rem;
            font-family: inherit;
            transition: border-color 0.2s ease;
        }
        
        input[type="text"]:focus,
        textarea:focus {
            outline: none;
            border-color: #667eea;
        }
        
        .output {
            background: #1f2937;
            color: #f9fafb;
            padding: 1.25rem;
            border-radius: 10px;
            min-height: 80px;
            font-family: 'Monaco', 'Courier New', monospace;
            font-size: 0.9rem;
            line-height: 1.6;
            white-space: pre-wrap;
            word-break: break-word;
        }
        
        .output.empty {
            color: #9ca3af;
            font-style: italic;
        }
        
        .success {
            color: #10b981;
            font-weight: 600;
        }
        
        .error {
            color: #ef4444;
            font-weight: 600;
        }
        
        .info {
            color: #3b82f6;
            font-weight: 600;
        }
        
        .warning {
            color: #f59e0b;
            font-weight: 600;
        }
        
        .status-list {
            background: #fff;
            padding: 1rem;
            border-radius: 8px;
            margin-bottom: 1rem;
        }
        
        .status-item {
            display: flex;
            justify-content: space-between;
            padding: 0.5rem 0;
            border-bottom: 1px solid #f3f4f6;
        }
        
        .status-item:last-child {
            border-bottom: none;
        }
        
        .status-label {
            color: #6b7280;
            font-size: 0.9rem;
        }
        
        .status-value {
            font-weight: 600;
            color: #1f2937;
        }
        
        @media (max-width: 768px) {
            .container {
                padding: 1.5rem;
            }
            
            h1 {
                font-size: 2rem;
            }
            
            .demo-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1>Lab 4: API Integration Demonstration</h1>
            <p class="subtitle">Vanilla JavaScript - Storage, Browser & Third-Party APIs</p>
            <span class="badge">Student: Md Jawar Safi (#2315024)</span>
        </header>

        <!-- STORAGE APIs -->
        <section class="section">
            <div class="section-header">
                <h2>1. Storage APIs</h2>
                <span class="section-badge">LocalStorage • SessionStorage • IndexedDB</span>
            </div>
            
            <div class="demo-grid">
                <!-- LocalStorage -->
                <div class="demo-card">
                    <h3>LocalStorage Demo</h3>
                    <p class="description">Persistent storage that survives browser restarts. Perfect for user preferences and settings.</p>
                    
                    <div class="instructions">
                        <h4>How to Use:</h4>
                        <ol>
                            <li>Select a theme below</li>
                            <li>Click "Save Theme"</li>
                            <li>Refresh page - theme persists!</li>
                        </ol>
                    </div>
                    
                    <select id="theme-select" style="width: 100%; padding: 0.875rem; border: 2px solid #e5e7eb; border-radius: 8px; margin-bottom: 1rem; font-size: 1rem;">
                        <option value="light">Light Mode</option>
                        <option value="dark">Dark Mode</option>
                        <option value="auto">Auto (System)</option>
                    </select>
                    
                    <button onclick="saveTheme()">Save Theme to LocalStorage</button>
                    <button onclick="loadTheme()" style="background: #6b7280;">Load Saved Theme</button>
                    
                    <div id="localStorage-output" class="output empty">Result will appear here...</div>
                </div>

                <!-- SessionStorage -->
                <div class="demo-card">
                    <h3>SessionStorage Demo</h3>
                    <p class="description">Temporary storage that clears when tab closes. Great for session-specific data.</p>
                    
                    <div class="instructions">
                        <h4>How to Use:</h4>
                        <ol>
                            <li>Enter your name below</li>
                            <li>Click "Save to Session"</li>
                            <li>Data persists until you close this tab</li>
                        </ol>
                    </div>
                    
                    <input type="text" id="session-input" placeholder="Enter your name..." />
                    <button onclick="saveSession()">Save to SessionStorage</button>
                    <button onclick="loadSession()" style="background: #6b7280;">Load from Session</button>
                    
                    <div id="sessionStorage-output" class="output empty">Result will appear here...</div>
                </div>

                <!-- IndexedDB -->
                <div class="demo-card">
                    <h3>IndexedDB Demo</h3>
                    <p class="description">Advanced database for storing large amounts of structured data, including files.</p>
                    
                    <div class="instructions">
                        <h4>How to Use:</h4>
                        <ol>
                            <li>Enter note title and content</li>
                            <li>Click "Save Note" to store in database</li>
                            <li>Click "Load All Notes" to view stored notes</li>
                            <li>Notes persist across sessions</li>
                        </ol>
                    </div>
                    
                    <input type="text" id="note-title" placeholder="Note title..." />
                    <textarea id="note-content" placeholder="Note content..." rows="3"></textarea>
                    <button onclick="saveNote()">Save Note to IndexedDB</button>
                    <button onclick="loadNotes()" style="background: #6b7280;">Load All Notes</button>
                    
                    <div id="indexedDB-output" class="output empty">Result will appear here...</div>
                </div>
            </div>
        </section>

        <!-- BROWSER APIs -->
        <section class="section">
            <div class="section-header">
                <h2>2. Browser APIs</h2>
                <span class="section-badge">Clipboard • Notifications • Geolocation</span>
            </div>
            
            <div class="demo-grid">
                <!-- Clipboard API -->
                <div class="demo-card">
                    <h3>Clipboard API Demo</h3>
                    <p class="description">Copy text to clipboard without user manually selecting. Modern replacement for document.execCommand.</p>
                    
                    <div class="instructions">
                        <h4>How to Use:</h4>
                        <ol>
                            <li>Type or paste text in the box below</li>
                            <li>Click "Copy to Clipboard"</li>
                            <li>Try pasting (Ctrl+V) anywhere to verify</li>
                        </ol>
                    </div>
                    
                    <textarea id="clipboard-input" placeholder="Enter text to copy..." rows="3"></textarea>
                    <button onclick="copyText()">Copy to Clipboard</button>
                    
                    <div id="clipboard-output" class="output empty">Result will appear here...</div>
                </div>

                <!-- Notifications API -->
                <div class="demo-card">
                    <h3>Notifications API Demo</h3>
                    <p class="description">Send desktop notifications to users even when page is not in focus.</p>
                    
                    <div class="instructions">
                        <h4>How to Use:</h4>
                        <ol>
                            <li>Click "Request Permission" first</li>
                            <li>Allow notifications when browser asks</li>
                            <li>Click "Send Notification" to test</li>
                            <li>You'll see a desktop notification!</li>
                        </ol>
                    </div>
                    
                    <button onclick="requestNotificationPermission()">Request Permission</button>
                    <button onclick="showNotification()" style="background: #10b981;">Send Test Notification</button>
                    
                    <div id="notification-output" class="output empty">Result will appear here...</div>
                </div>

                <!-- Geolocation API -->
                <div class="demo-card">
                    <h3>Geolocation API Demo</h3>
                    <p class="description">Get user's geographic coordinates with permission. Useful for location-based features.</p>
                    
                    <div class="instructions">
                        <h4>How to Use:</h4>
                        <ol>
                            <li>Click "Get My Location"</li>
                            <li>Allow location access when prompted</li>
                            <li>View your coordinates and accuracy</li>
                        </ol>
                    </div>
                    
                    <button onclick="getLocation()">Get My Location</button>
                    
                    <div id="location-output" class="output empty">Result will appear here...</div>
                </div>
            </div>
        </section>

        <!-- THIRD-PARTY APIs -->
        <section class="section">
            <div class="section-header">
                <h2>3. Third-Party APIs</h2>
                <span class="section-badge">Weather • Quotes • GitHub</span>
            </div>
            
            <div class="demo-grid">
                <!-- Weather API -->
                <div class="demo-card">
                    <h3>Weather API Demo</h3>
                    <p class="description">Fetch real-time weather data using Open-Meteo API (free, no API key required).</p>
                    
                    <div class="instructions">
                        <h4>How to Use:</h4>
                        <ol>
                            <li>Click "Get Weather" below</li>
                            <li>Allow location access</li>
                            <li>View current weather conditions</li>
                        </ol>
                    </div>
                    
                    <button onclick="getWeather()">Get Weather for My Location</button>
                    
                    <div id="weather-output" class="output empty">Result will appear here...</div>
                </div>

                <!-- Quotes API -->
                <div class="demo-card">
                    <h3>Quotes API Demo</h3>
                    <p class="description">Fetch random inspirational quotes from Quotable.io API (free, no API key required).</p>
                    
                    <div class="instructions">
                        <h4>How to Use:</h4>
                        <ol>
                            <li>Click "Get Random Quote"</li>
                            <li>View a random inspirational quote</li>
                            <li>Click again for a new quote</li>
                        </ol>
                    </div>
                    
                    <button onclick="getQuote()">Get Random Quote</button>
                    
                    <div id="quote-output" class="output empty">Result will appear here...</div>
                </div>

                <!-- GitHub API -->
                <div class="demo-card">
                    <h3>GitHub API Demo</h3>
                    <p class="description">Fetch public GitHub user profiles using the GitHub REST API (no authentication needed for public data).</p>
                    
                    <div class="instructions">
                        <h4>How to Use:</h4>
                        <ol>
                            <li>Enter any GitHub username</li>
                            <li>Click "Get User Profile"</li>
                            <li>View user details (followers, repos, etc.)</li>
                        </ol>
                    </div>
                    
                    <input type="text" id="github-input" placeholder="Enter GitHub username..." value="786jabar" />
                    <button onclick="getGitHubUser()">Get User Profile</button>
                    
                    <div id="github-output" class="output empty">Result will appear here...</div>
                </div>
            </div>
        </section>
    </div>

    <script type="module">
        import { LocalStorageDemo, SessionStorageDemo, IndexedDBDemo } from './js/storage.js';
        import { ClipboardAPI, NotificationsAPI, GeolocationAPI } from './js/browser.js';
        import { WeatherAPI, QuotesAPI, GitHubAPI } from './js/thirdparty.js';

        // Initialize IndexedDB
        await IndexedDBDemo.init();
        console.log('[OK] Lab 4 vanilla JavaScript modules loaded successfully!');

        // === LOCALSTORAGE FUNCTIONS ===
        window.saveTheme = async function() {
            const theme = document.getElementById('theme-select').value;
            const result = await LocalStorageDemo.saveItem('theme', theme);
            const outputEl = document.getElementById('localStorage-output');
            
            if (result.success) {
                outputEl.className = 'output';
                outputEl.innerHTML = `<span class="success">[OK] Theme Saved!</span>\n\nKey: "theme"\nValue: "${theme}"\n\nThis data persists even after you close the browser!`;
            } else {
                outputEl.className = 'output';
                outputEl.innerHTML = `<span class="error">[ERROR] ${result.message}</span>`;
            }
        };

        window.loadTheme = async function() {
            const result = await LocalStorageDemo.getItem('theme');
            const outputEl = document.getElementById('localStorage-output');
            
            if (result.success && result.data) {
                document.getElementById('theme-select').value = result.data;
                outputEl.className = 'output';
                outputEl.innerHTML = `<span class="success">[OK] Theme Loaded!</span>\n\nStored Value: "${result.data}"\n\nThe theme preference was retrieved from LocalStorage and applied to the dropdown!`;
            } else {
                outputEl.className = 'output';
                outputEl.innerHTML = `<span class="warning">[INFO] No saved theme found</span>\n\nPlease select and save a theme first.`;
            }
        };

        // === SESSIONSTORAGE FUNCTIONS ===
        window.saveSession = async function() {
            const name = document.getElementById('session-input').value.trim();
            const outputEl = document.getElementById('sessionStorage-output');
            
            if (!name) {
                outputEl.className = 'output';
                outputEl.innerHTML = `<span class="warning">[WARNING] Please enter your name!</span>`;
                return;
            }
            
            const sessionData = {
                name: name,
                timestamp: new Date().toISOString(),
                visits: (parseInt(sessionStorage.getItem('visitCount')) || 0) + 1
            };
            
            const result = await SessionStorageDemo.saveItem('user', JSON.stringify(sessionData));
            sessionStorage.setItem('visitCount', sessionData.visits);
            
            if (result.success) {
                outputEl.className = 'output';
                outputEl.innerHTML = `<span class="success">[OK] Session Data Saved!</span>\n\nName: ${sessionData.name}\nVisits this session: ${sessionData.visits}\nTimestamp: ${new Date(sessionData.timestamp).toLocaleString()}\n\nThis data will clear when you close this tab.`;
            }
        };

        window.loadSession = async function() {
            const result = await SessionStorageDemo.getItem('user');
            const outputEl = document.getElementById('sessionStorage-output');
            
            if (result.success && result.data) {
                const data = JSON.parse(result.data);
                outputEl.className = 'output';
                outputEl.innerHTML = `<span class="success">[OK] Session Data Retrieved!</span>\n\nName: ${data.name}\nSaved at: ${new Date(data.timestamp).toLocaleString()}\nVisits: ${data.visits}\n\nThis data exists only for this browser tab/window.`;
            } else {
                outputEl.className = 'output';
                outputEl.innerHTML = `<span class="warning">[INFO] No session data found</span>\n\nPlease enter your name and save first.`;
            }
        };

        // === INDEXEDDB FUNCTIONS ===
        window.saveNote = async function() {
            const title = document.getElementById('note-title').value.trim();
            const content = document.getElementById('note-content').value.trim();
            const outputEl = document.getElementById('indexedDB-output');
            
            if (!title || !content) {
                outputEl.className = 'output';
                outputEl.innerHTML = `<span class="warning">[WARNING] Please fill in both title and content!</span>`;
                return;
            }
            
            const note = {
                id: Date.now(),
                title: title,
                content: content,
                createdAt: new Date().toISOString()
            };
            
            const result = await IndexedDBDemo.saveNote(note);
            
            if (result.success) {
                document.getElementById('note-title').value = '';
                document.getElementById('note-content').value = '';
                outputEl.className = 'output';
                outputEl.innerHTML = `<span class="success">[OK] Note Saved to IndexedDB!</span>\n\nID: ${note.id}\nTitle: "${title}"\nContent: "${content}"\nCreated: ${new Date(note.createdAt).toLocaleString()}\n\nClick "Load All Notes" to see all stored notes.`;
            } else {
                outputEl.className = 'output';
                outputEl.innerHTML = `<span class="error">[ERROR] ${result.message}</span>`;
            }
        };

        window.loadNotes = async function() {
            const result = await IndexedDBDemo.getAllNotes();
            const outputEl = document.getElementById('indexedDB-output');
            
            if (result.success) {
                if (result.data.length === 0) {
                    outputEl.className = 'output';
                    outputEl.innerHTML = `<span class="info">[INFO] No notes in database</span>\n\nSave a note first to see it appear here!`;
                } else {
                    outputEl.className = 'output';
                    outputEl.innerHTML = `<span class="success">[OK] Found ${result.data.length} note(s)!</span>\n\n` + 
                        result.data.map((note, i) => 
                            `Note #${i + 1}:\n  Title: "${note.title}"\n  Content: "${note.content}"\n  Created: ${new Date(note.createdAt).toLocaleString()}`
                        ).join('\n\n');
                }
            } else {
                outputEl.className = 'output';
                outputEl.innerHTML = `<span class="error">[ERROR] ${result.message}</span>`;
            }
        };

        // === CLIPBOARD FUNCTION ===
        window.copyText = async function() {
            const text = document.getElementById('clipboard-input').value;
            const outputEl = document.getElementById('clipboard-output');
            
            if (!text) {
                outputEl.className = 'output';
                outputEl.innerHTML = `<span class="warning">[WARNING] Please enter some text to copy!</span>`;
                return;
            }
            
            const result = await ClipboardAPI.copyText(text);
            
            if (result.success) {
                outputEl.className = 'output';
                outputEl.innerHTML = `<span class="success">[OK] Text Copied to Clipboard!</span>\n\nCopied: "${text}"\n\nTry pasting (Ctrl+V or Cmd+V) in any text field to verify it worked!`;
            } else {
                outputEl.className = 'output';
                outputEl.innerHTML = `<span class="error">[ERROR] ${result.message}</span>`;
            }
        };

        // === NOTIFICATIONS FUNCTIONS ===
        window.requestNotificationPermission = async function() {
            const outputEl = document.getElementById('notification-output');
            const permission = await NotificationsAPI.requestPermission();
            
            if (permission === 'granted') {
                outputEl.className = 'output';
                outputEl.innerHTML = `<span class="success">[OK] Permission Granted!</span>\n\nYou can now receive desktop notifications from this site.\n\nClick "Send Test Notification" to try it!`;
            } else if (permission === 'denied') {
                outputEl.className = 'output';
                outputEl.innerHTML = `<span class="error">[ERROR] Permission Denied</span>\n\nYou blocked notifications. To enable:\n\n1. Click the lock icon in address bar\n2. Find "Notifications"\n3. Change to "Allow"\n4. Refresh this page`;
            } else {
                outputEl.className = 'output';
                outputEl.innerHTML = `<span class="info">[INFO] Permission not determined</span>\n\nPlease click "Allow" when browser prompts you.`;
            }
        };

        window.showNotification = async function() {
            const outputEl = document.getElementById('notification-output');
            const permission = await NotificationsAPI.requestPermission();
            
            if (permission === 'granted') {
                const success = NotificationsAPI.showSuccess('Lab 4 Demo', {
                    body: 'This is a test notification from the vanilla JavaScript demo!',
                    icon: 'data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><text y="75" font-size="75">📚</text></svg>'
                });
                
                if (success) {
                    outputEl.className = 'output';
                    outputEl.innerHTML = `<span class="success">[OK] Notification Sent!</span>\n\nCheck your desktop/system notifications panel.\n\nIf you don't see it, the notification might have appeared briefly in the corner of your screen.`;
                }
            } else {
                outputEl.className = 'output';
                outputEl.innerHTML = `<span class="error">[ERROR] Permission required</span>\n\nClick "Request Permission" first and allow notifications.`;
            }
        };

        // === GEOLOCATION FUNCTION ===
        window.getLocation = async function() {
            const outputEl = document.getElementById('location-output');
            outputEl.className = 'output';
            outputEl.innerHTML = `<span class="info">[INFO] Getting your location...</span>\n\nPlease allow location access when prompted.`;
            
            try {
                const position = await GeolocationAPI.getCurrentPosition();
                const formatted = GeolocationAPI.formatCoordinates(position.latitude, position.longitude);
                
                outputEl.innerHTML = `<span class="success">[OK] Location Found!</span>\n\nLatitude: ${position.latitude}\nLongitude: ${position.longitude}\nAccuracy: ±${position.accuracy.toFixed(0)} meters\n\nFormatted: ${formatted}\n\nYou can use these coordinates with mapping services or the Weather API below!`;
            } catch (error) {
                outputEl.innerHTML = `<span class="error">[ERROR] ${error.message}</span>\n\nCommon issues:\n• Location services disabled\n• Permission denied\n• Timeout (took too long)\n\nEnable location in your browser settings and try again.`;
            }
        };

        // === WEATHER FUNCTION ===
        window.getWeather = async function() {
            const outputEl = document.getElementById('weather-output');
            outputEl.className = 'output';
            outputEl.innerHTML = `<span class="info">[INFO] Getting weather...</span>\n\nStep 1: Getting your location...\nStep 2: Fetching weather data...`;
            
            try {
                const position = await GeolocationAPI.getCurrentPosition();
                const result = await WeatherAPI.getWeatherByCoords(position.latitude, position.longitude);
                
                if (result.success) {
                    const w = result.data;
                    outputEl.innerHTML = `<span class="success">[OK] Weather Data Loaded!</span>\n\n${w.description}\n\nTemperature: ${w.temperature}°C\nWind Speed: ${w.windspeed} km/h\nUpdated: ${new Date(w.time).toLocaleString()}\n\nData provided by Open-Meteo API (free weather service)`;
                } else {
                    outputEl.innerHTML = `<span class="error">[ERROR] Weather API failed</span>\n\n${result.error}\n\nPlease try again in a moment.`;
                }
            } catch (error) {
                outputEl.innerHTML = `<span class="error">[ERROR] Location required</span>\n\n${error.message}\n\nWeather needs your location first. Please allow location access and try again.`;
            }
        };

        // === QUOTES FUNCTION ===
        window.getQuote = async function() {
            const outputEl = document.getElementById('quote-output');
            outputEl.className = 'output';
            outputEl.innerHTML = `<span class="info">[INFO] Fetching inspirational quote...</span>\n\nConnecting to Quotable.io API...`;
            
            const result = await QuotesAPI.getRandomQuote();
            
            if (result.success) {
                const q = result.data;
                outputEl.innerHTML = `<span class="success">[OK] Quote Received!</span>\n\n"${q.content}"\n\n— ${q.author}\n\nTags: ${q.tags.join(', ')}\n\nClick "Get Random Quote" again for a new quote!`;
            } else {
                outputEl.innerHTML = `<span class="error">[ERROR] Failed to fetch quote</span>\n\n${result.error || 'Unknown error'}\n\nThe Quotable API might be temporarily unavailable. Try again in a moment.`;
            }
        };

        // === GITHUB FUNCTION ===
        window.getGitHubUser = async function() {
            const username = document.getElementById('github-input').value.trim();
            const outputEl = document.getElementById('github-output');
            
            if (!username) {
                outputEl.className = 'output';
                outputEl.innerHTML = `<span class="warning">[WARNING] Please enter a GitHub username!</span>`;
                return;
            }
            
            outputEl.className = 'output';
            outputEl.innerHTML = `<span class="info">[INFO] Fetching @${username}'s profile...</span>\n\nConnecting to GitHub API...`;
            
            const result = await GitHubAPI.getUser(username);
            
            if (result.success) {
                const u = result.data;
                outputEl.innerHTML = `<span class="success">[OK] Profile Loaded!</span>\n\nUsername: @${u.username}\nName: ${u.name || 'Not set'}\nBio: ${u.bio || 'No bio'}\n\nFollowers: ${u.followers}\nPublic Repos: ${u.repos}\nLocation: ${u.location || 'Not specified'}\n\nProfile URL: ${u.url}`;
            } else {
                outputEl.innerHTML = `<span class="error">[ERROR] User not found</span>\n\nUsername "${username}" doesn't exist on GitHub.\n\nPlease check the spelling and try again.`;
            }
        };

        // Auto-load theme on page load
        window.addEventListener('load', () => {
            loadTheme();
        });
    </script>
</body>
</html>
